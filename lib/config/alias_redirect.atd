
type database <ocaml from="Database" t="t"> = abstract

type cache = {
	max_record_age: float;
	target_length: int;
	trim_length: int;
}

type t = {
	port: int;
	database: database;
	cache: cache;
	?pathless_redirect_uri: string option;
}